#include <iostream>
#include <string>
#include <vector>

using std::cin, std::cout, std::endl, std::getline, std::string, std::vector;

int main() {

	// If we didn't initialize the scores array on page 116, the program would result in undefined behavior on
	// some machines, assuming the scores array was was default initialized inside the main function.
	// Default initializing variables inside a function results in undefined values.


	return 0;
}
