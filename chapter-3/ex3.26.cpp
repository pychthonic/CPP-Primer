#include <iostream>
#include <string>
#include <vector>

using std::cin, std::cout, std::endl, std::getline, std::string, std::vector;

int main() {

	// (beg + end) / 2 will possibly go past the bounds of the vector, even if dividing by two
	// brings it back within the bounds, so this would result in undefined behavior.


	return 0;
}
