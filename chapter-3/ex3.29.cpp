#include <iostream>
#include <string>
#include <vector>

using std::cin, std::cout, std::endl, std::getline, std::string, std::vector;

int main() {
	// Arrays can only be used if we know how many elements the data structure will hold 
	// at compile time.



	return 0;
}
