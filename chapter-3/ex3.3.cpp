#include <iostream>
#include <string>

using std::cin, std::cout, std::endl, std::getline;

int main() {

	// std::cin removes leading whitespace, reads characters until it encounters a space,
	// and copies those characters into a string.
	// std::getline does the same except it reads in spaces and stops at a newline character.


	return 0;
}
